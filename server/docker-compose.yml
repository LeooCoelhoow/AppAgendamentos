version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1734314098Leo#
      POSTGRES_DB: TESTE_LOGIN_DB
    ports:
      - "5433:5433"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data